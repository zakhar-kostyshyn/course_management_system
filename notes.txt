Plan

1) Set variables from .env file to be accessible in application.yml
2) Write bash script or make a config in docker-compose.yml to make possible start liquibase  after db up
        - HealthCheck

3) Create simple test for db connection
        - Create test database during gradle build
        - Data in test DB should be the same as in prod
        - For test run migration and then populate in test cases

5) Create simple endpoint to test that everything working correctly
6) Deploy App to AWS and check simple endpoint

4) Create GitHub/CircleCI configuration to run tests before pushing to master
    -- Push to prod

7) Continue with programming tasks
